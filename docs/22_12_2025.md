# Changelog - 22/12/2025

## 🐞 Sửa lỗi (Bug Fix)

### Kanban Board: Ticket không hiển thị

- **Vấn đề:** Các ticket tồn tại trong danh sách ticket của dự án nhưng không hiển thị trên các cột của Kanban board.
- **Nguyên nhân:** Lỗi logic ở frontend trong file `resources/js/stores/kanban.js`. Logic dùng để lọc và gán ticket vào các cột trạng thái đã so sánh sai (so sánh một object `status` với một `id`), dẫn đến việc không có ticket nào được khớp và hiển thị.
- **Giải pháp:** Sửa lại logic filter để so sánh chính xác `id` của trạng thái trong ticket với `id` của cột (`ticket.status.id === status.id`).

## 📝 Thay đổi chi tiết

- **File đã sửa:** `resources/js/stores/kanban.js`
- **Logic cũ:** `tasks: tickets.filter(ticket => (ticket.status_id || ticket.status) === status.id)`
- **Logic mới:** `tasks: tickets.filter(ticket => ticket.status && ticket.status.id === status.id)`
